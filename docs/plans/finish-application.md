# Finish the application

Goal: complete the remaining original product behavior in the existing Rust/GPUI application and produce a usable Linux package.

Implement directly in this worktree. Keep changes in feature-sized commits, run affected tests once per meaningful change, and reuse existing successful evidence until a source change or new failure makes it stale. Existing GPU reviews stay valid within their recorded source scope; missing native hardware remains unverified.

## Todo

- [x] Inspect the product specification and current source; verify the remaining feature gaps against the implementation.
- [x] Process search/sort, pointer selection and identity-bound End task/Force quit with confirmation and visible errors; affected Rust/GPUI tests and strict Clippy pass. Native desktop replay remains in the final acceptance item.
- [x] Docked settings, bundled fonts and dark/light appearance with durable state; affected tests and strict Clippy pass.
- [x] Named preset CRUD, protected built-ins and legacy single-preset import; affected tests and strict Clippy pass.
- [x] Context menus for monitor/sensor visibility, ordering and explicit compatible meter selection; affected tests and strict Clippy pass.
- [x] Balanced first-launch layout, CPU core tiles and physical memory composition; affected tests, strict Clippy and native fresh/minimum-window and appearance smoke pass.
- [x] Linux package, launcher, icon, installation/removal instructions and isolated packaged launch; installer tests and installed launch/restart/removal smoke pass.
- [ ] **In progress:** Full application/native acceptance, unchanged Linux preservation and final review. Retain the separate missing Intel/Mac evidence and Mac F1 obligations.

Exactly one todo is active. A feature is checked only after its implementation and affected verification pass. GPU Task 5 is pending within this last item, not a second concurrent implementation task.

## Implementation boundaries and checks

1. Process projection belongs in `examples/system_pulse/src/processes.rs`; it uses raw `ProcessRow` numeric values, never formatted strings. Keep stable PID/start identity through filter/sort and snapshots. Add focused projection tests for numeric ordering, unavailable values, case-insensitive search and selected identity. Integrate the existing virtual list in `panel.rs` with a real input, focusable headers, pointer selection and native context menu. Keep the reviewed deferred identity reveal.
2. Process operations belong in `examples/system_pulse/collectors/src/process_control.rs`, separate from sampling. Linux acquires a pidfd, verifies the process start identity against procfs and signals that handle. Block PID 0/1 and this application. Unsupported platforms fail explicitly. A single background task executes an explicitly confirmed operation; tests use only spawned children and prove wrong identity/cancellation never sends a signal. Preserve permission and process-exit errors.
3. Appearance and named preset data belong in the model (`settings.rs`, `presets.rs`) and existing `Workspace` serialization with backward-compatible defaults. Use bounded, validated preset names and a separate atomic preset-library file; preserve unreadable/corrupt originals. Existing single `preset.json` imports as a named legacy preset without deleting it. Presets capture the whole workspace, and built-ins are templates resolved against current hardware.
4. Build a `SettingsPanel` view under `src/settings.rs` inside the existing monitor dock identity. Use existing gpui-component inputs, buttons, menus and theme tokens. UI/mono font roles use embedded licensed assets with at least two curated choices per role. Theme and interval changes apply immediately and save through existing debounced persistence.
5. Keep context actions and sensor ordering attached to stable IDs. Add model operations for moving a sensor and selecting a compatible meter; retain hidden/collapsed states. Use existing native context-menu components, clear action labels and keyboard-accessible controls.
6. A `layout.rs` module builds Default, Minimal, GPU Focus and Developer dock templates. First-launch defaults only apply when no saved workspace exists. CPU/Memory/Processes are primary; first GPU joins when discovered. Disk/network start hidden. Existing saved dock JSON always wins. Render the actual per-core percentage sensors as a compact grid and actual memory composition operands as a labeled composition bar; retain unavailable states.
7. Package the release binary, licenses and desktop integration under a task-owned output directory outside the checkout. Installer defaults to the user's local prefix, never touches saved state, and supports an isolated test prefix. Embed assets so launch does not depend on the checkout. Verify fresh state, normal shutdown, missing input and configuration preservation.
8. Run affected model/collector/app tests, formatting and strict Clippy during each feature. Extend existing Python native replay for search, sort, safe task-owned process actions, context menus, settings, presets and restart. Retain an actual failing case before implementing each nontrivial behavior. Run the final committed aggregate once after all features and source review; preserve failures and fix demonstrated causes. Do not weaken native freshness, identity or physical-value checks.

## Current evidence and limits

The starting tree passed [870 tests and full Linux preservation](../execution/intel-and-apple-gpus/linux-preservation-pass.md). The product gaps are directly visible in `src/panel.rs` (static process headers and Settings placeholder), `src/workspace.rs` (one preset slot and all-monitor vertical default), and `model/src/presentation.rs` (no appearance or named presets). Paths are relative to `examples/system_pulse/` unless fully qualified above. Source edits will make relevant prior acceptance stale and require the final replay.

Process slice verification (2026-09-06): `cargo test --locked -p system-pulse -p system-pulse-collectors --lib` passed 72 application and 98 collector tests. Focused GPUI tests exercise pointer selection, search, header sorting, confirmation identity, Cancel and visible fixture-operation rejection. Backend tests signal only task-owned child processes and reject wrong/exited/protected identities. `cargo fmt -p system-pulse -p system-pulse-collectors` and strict all-target Clippy for both packages passed. Actual native replay and committed Cairn evidence remain pending in final application acceptance.

Settings slice verification (2026-09-06): `cargo test --locked -p system-pulse -p system-pulse-model --all-targets` passed 100 tests (74 app, 26 model); native macOS cases correctly reported unavailable on Linux. GPUI tests click the actual docked theme/font/interval controls, verify live theme state, reveal offscreen keyboard focus and restore serialized choices. Fontconfig decoded the four embedded families; sources, hashes and original OFL notices are bundled. Package formatting and strict all-target Clippy passed. Native screenshots and isolated packaged font loading remain in final acceptance.

Preset slice verification (2026-09-06): 107 application/model tests passed with `cargo test --locked -p system-pulse -p system-pulse-model --all-targets`; native macOS cases remain unavailable. Tests cover independent named workspaces, case-insensitive collisions, protected built-ins, invalid input, CRUD and serialization, legacy import without altering the original, malformed-library preservation and actual GPUI confirmation/cancel/rename/recall/delete controls. All four templates pass the separate-panel dock validator. A caught reentrant entity-update failure was fixed by deferring manager commands until their event update releases its borrow. Formatting and strict all-target Clippy passed. Native library persistence and packaged replay remain in final acceptance.

Context-control slice verification (2026-09-06): 110 application/model tests passed with the all-target command; strict all-target Clippy and formatting passed. Model tests preserve hidden/collapsed identity during reordering and reject incompatible meter choices. The GPUI replay opens the actual right-click sensor menu, selects a meter with keyboard input, then uses the options menu to reorder/hide and verifies restored state. The failing replay exposed nested workspace and sensor menus opening together; the workspace menu now belongs to the title, while rows and monitor headers own their respective menus. Native platform replay remains in final acceptance.

Dashboard slice verification (2026-09-06): 114 application/model tests passed (82 app, 32 model), with formatting and strict all-target Clippy. Physical composition rejects missing, stale and differently timed operands; the first-launch initializer preserves appearance and leaves saved choices alone. GPUI tests exercise contiguous core tiles, unavailable readings, collapse, selected meters, movement across a non-core row and the Settings shortcut's body reveal. Native fresh launch at 1280×880, resize to 960×640, Settings shortcut, Light mode and normal shutdown passed in `/home/shawn/workspace2/task-manager-artifacts/finish-application/dashboard-appearance-20260907`; earlier failed smoke attempts remain alongside it. The private native replay now explicitly positions its owned window at the origin before using window-relative pointer coordinates, accommodating the centered production window. Its six affected publication/transport tests and Ruff passed. The final committed preservation run remains pending.

Linux package slice verification (2026-09-07): the release build passed and produced a 25,300,495-byte archive at `/home/shawn/workspace2/task-manager-artifacts/finish-application/package-20260907/system-pulse-0.1.0-linux-x86_64-6d490fc3c500.tar.gz` (SHA-256 `3b7bc646a695a2036b8b5617c17cf1ebee0fc7b00588bc3cf319983859dc9b7b`). It identifies binary source `6d490fc3c500c7eaf42a26a4464a73193b192156`, bundles its source archive and resolves notices for 540 build dependencies with cargo-about 0.9.2. Two upstream manifest omissions are clarified against their shipped Apache notice hashes. Five installer tests, six affected transport tests, 17 native-input tests, Ruff, Rust formatting and strict all-target Clippy passed. The 114 application/model tests passed during packaging development.

Installed smoke passed at `/home/shawn/workspace2/task-manager-artifacts/finish-application/package-validation-persisted-20260907/evidence`: extraction, repeated installation under a prefix containing spaces, desktop validation and GLib argument parsing, launch after removing the extracted files and changing to an empty directory, first/minimum captures, bundled assets, Light mode, restart, normal shutdown and removal that preserved actual workspace bytes plus unowned files. The owner reaped all task descendants. Earlier failed artifacts remain; the smoke now waits for the requested persisted appearance instead of accepting an unrelated pending autosave as its acknowledgement. Final acceptance will rebuild the handover archive against the final committed source and retain the separate hardware limitations.

Native product preparation (2026-09-07): the complete isolated replay passed in `/home/shawn/workspace2/task-manager-artifacts/finish-application/application-native-status-20260907/evidence`. It exercised case-insensitive name/PID search, physical PID sorting with retained selection, native context Cancel, SIGTERM/SIGKILL against owned children, an accessible protected-process error, sensor movement/meter/collapse/visibility, monitor visibility, fonts/theme/interval, two named workspaces, rename, overwrite/delete with cancellation, builtin/named recall and restart. Earlier failed runs remain beside it. Native focus on the input frame previously failed to reach the editor; an explicit Focus action now forwards to its editor. Process and preset status messages now expose their visible text as native labels. Ten existing input tests, 82 application tests, component strict all-target Clippy, Ruff and formatting passed. Final committed aggregate and review are still pending.

The first committed product aggregate at `a94e1f7d12809067d50a78bc86a286dcaec984bb` passed 901 tests and all formatting checks, then Clippy stopped with an internal rustc `uninterned StableCrateId(0)` error while promoting cached gpui-component metadata. The failed evidence is retained at `/home/shawn/workspace2/task-manager-artifacts/finish-application/final-acceptance-20260907`; Cairn correctly recorded APP/LIVE unverified. Nine generated gpui-component incremental-cache directories were moved intact to `compiler-cache-recovery-20260907` beside it. The identical full strict Clippy command then passed without source or compiler-option changes. The complete gate will rerun from fresh output.
