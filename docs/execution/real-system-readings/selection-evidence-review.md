# Selection and current exit evidence review

Candidate: `1790fe53506439b8c897421aa91be15598586162`.

Independent SPEC and QUALITY PASS. Each ran all 140 native Python tests.
SPEC also ran five probes for selected PID reuse, post-scan registration
loss, snapshot prerequisites, and deadline exhaustion. QUALITY ran four
probes, including the original detached-cache false success. All passed.

Exit proof discovers the unique current panel, completes strict scanning,
rejects a foreign selected identity even after a retained target row, and
validates post-scan membership under the original five-second deadline.
Native success describes target absence and no other instantiated selected
row. It does not claim visibility into unrendered model state.

Both reviewers inspected the GPUI integration test: it checks accepted
identities and exact panel.selected through production accept_snapshot
after reordering, removal, and PID reuse, before further input. The worker
ran that test and all 49 app tests, all 244 Python tests, Clippy, Ruff, and
rustfmt successfully. Reviewers did not rerun Rust tests or start live runs.

The model-clear wording and detached-panel verifier findings are closed.
Fresh native/full acceptance and final commitment review remain pending.
