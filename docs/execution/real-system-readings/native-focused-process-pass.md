# Focused native process acceptance

Fresh untraced capture `pulse-process-lookup-6z8tnj4m/native` passed at
source `b1663306393a7183b90ab0c50ed05deafaddb334`. The [artifact record](native-focused-process-pass.json)
retains metadata, paths, hashes and extracted observations. This is focused
process acceptance, not the full committed aggregate or final review.

Launch, live metrics, collapse, charts, inner scrolling and the process
case passed. The controlled child was PID/start `3971079:74489217`.
All sixteen child cell comparisons passed, including eight initial cell
checks and eight visible comparisons across columns 0–7. After three fresh
sequences, current strict selection was acknowledged before shutdown.
The native exit proof passed, and an independent subsequent stat read
returned ENOENT for the controlled PID.

Held Up, exact-64-Up, and horizontal movement retained joined observers
and at least three distinct fresh sequences each. Their maximum observed
ages were 1.320, 1.255, 1.361 and 1.271 seconds respectively, within the
unchanged two-second limit. The exact-64-Up endpoint matched the expected
PID/start identity.

Child navigation used two arrow batches. One eligible intermediate batch
retained an independent matching stat baseline: the raw read took 52,039 ns
and the complete helper query took 133,972 ns in this capture. No batch
interruption or visibility-recovery event occurred. This run therefore does
not establish live recovery-path timing or retrospectively explain earlier
failures; those paths retain their separate regression evidence.

The running executable and recorded binary hashes matched. The application
closed normally with exit code zero, and its PID was absent at cleanup.
The private transport also exited zero without forced kill and was absent.

Full automated, host and native aggregate acceptance, final adversarial
review and Cairn Done remain pending. NVIDIA hardware accuracy, native
macOS/Windows behavior and physical device removal remain unverified.
