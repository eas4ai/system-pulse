# Prior acknowledged selection: independent navigation diagnosis

The [u12hxbrp replay](native-inspection-eligibility-focused-result.md) remains
FAIL. Independent reviewer `review_publication_quality` inspected frozen source,
retained observations, the screenshot and implementation history without edits,
builds or live runs.

## What the evidence establishes

Observation 165 retained coherent positive selection proof for
`process:3023548:64910375`, followed by the journal acknowledgement before the
next dispatch. Observations 166 and 167 selected that exact identity in the
issued publication. Neither observed a different competitor. The failed batch's
47 observations comprise those two prior selections, forty no-selection scans
and five changing-publication rejections. All scans completed.

Observations 210–212 match the failure frame's full PID, sequence 135, revision
134 and acceptance timestamp. The snapshot owns its process vector through Arc,
and diagnostic publication advances the revision, so this tuple joins the
retained process membership to those observations. The expected row is at 1298,
outside complete span 1299–1307; the issued reference index 1299 lies inside.
The permanent flag set at observation 166 prevents recovery qualification.

That is not proof of successful counterfactual recovery. Fresh global uniqueness,
clipping and final pre-dispatch checks were never reached. The screenshot does
not prove hidden model selection or an exact endpoint acknowledgement.

## Narrow corrective direction

For intermediate pending arrow batches only, distinguish an initial prefix of
the exactly prior positively acknowledged selection in the same issued publication
from a different competing selection. Freeze prior ACK identity/index/publication
from the already returned coherent native proof and bind it to this batch's
issued publication, expected identity, keys and dispatch. Do not derive this
permission from diagnostic rendered selection or the observation history.

The exception only prevents that prefix from setting a new permanent block.
It grants no input or acknowledgement and never clears an existing block. It
expires on a coherent different selection/absence or publication advance.
Distinct competitors and an instantiated unselected expected row retain their
permanent blocking behavior. Missing or mismatched prior evidence cannot enable
the exception. Home/End, reconciliation, final-target batches, inspection,
pre-exit and held input retain their current behavior.

After the prefix ends, every existing fresh complete strict coherent absence,
identity, reindex/span, global uniqueness, clipped-viewport, final publication/
membership and pre-input deadline guard remains mandatory before nonselecting
reveal. Exact successful endpoint acknowledgement still requires fresh strict
selected identity afterward. All original batch/total deadlines, freshness/PID
checks, controlled child comparisons and exit proof remain unchanged.

Record a separate Judged decision before implementation. Test the retained
sequence and all binding, expiry, hard-block, rejection and default-scope cases,
then independent SPEC and QUALITY review. No live cause outside this demonstrated
blocking decision is inferred. Fresh full aggregate acceptance and final review
remain required.
