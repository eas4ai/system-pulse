# Native pacing review

Reviewed directly against APP-008, LIVE-003, LIVE-010, LIVE-013 and the bounded-arrow pacing decision on 2026-09-07. No code changed during this review.

The retained native failure exhausted 180 seconds while exact two-key acknowledgements continued toward the live controlled target after host PID wrap. The four new tests use the actual navigation method and validators. Before the correction, both distant-target fixtures exhausted 180 seconds and an eight-key prior binding was rejected. After it, all four tests and the complete 277-test native-harness suite passed. The dropped eighth key produces a wrong endpoint, expires the existing batch deadline, and never authorizes a second batch.

The source diff changes dispatch size only when distance exceeds 32. It leaves the final approach, Home/End, target selection, metric inspection, exit and held-input calls intact. Successful batches retain exact native endpoint proof. The shared key validator rejects empty, mixed and over-eight envelopes at prior capture, frozen dispatch validation and prior-prefix construction; actual dispatched keys must still equal the frozen keys. Prior-prefix recognition only suppresses an initial competing-selection block and still expires without acknowledging anything. Existing strict current membership, unique selection, independent exit proof, snapshot freshness, journal/dispatch checks and deadlines remain in place. Existing malformed, stale, detached, competitor, missing-target, reuse, recovery and deadline tests passed unchanged.

This is a harness pacing change, with no product source changes. The final aggregate must still pass all sixteen child metric comparisons, controlled exit, exact 64-key input, held input, all other native cases and the packaged flows. The earlier failed captures remain failed. Native hardware and Mac F1 obligations are unchanged. No finding remains in this bounded source review; it is not a final acceptance or cross-platform pass.
