version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly"
    # Zed and AccessKit updates must carry the local native adapter patches.
    ignore:
      - dependency-name: "gpui*"
      - dependency-name: "accesskit*"
